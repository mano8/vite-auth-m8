# Preact Chrome MV3 extension

Preact extension using the package provider, hooks, and default auth views.

```bash
npm run build
npm run build:example:preact
```

Load `examples/preact-extension/dist/` from `chrome://extensions`.
