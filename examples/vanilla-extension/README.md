# Vanilla Chrome MV3 extension

Minimal Chrome extension using the framework-neutral runtime and vanilla DOM
adapter.

```bash
npm run build
npm run build:example:vanilla
```

Load `examples/vanilla-extension/dist/` from `chrome://extensions`.
