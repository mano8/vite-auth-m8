# React Chrome MV3 extension

React extension using the package provider, hooks, and default auth views.

```bash
npm run build
npm run build:example:react
```

Load `examples/react-extension/dist/` from `chrome://extensions`.
